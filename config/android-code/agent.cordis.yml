# The `cordis` agent preset: the standard coding agent, plus the ability to
# read and write the runtime it is running in.
#
# It exists so a person can ask an agent to author another agent. Everything in
# `standard` is here unchanged; what is added is the self-referential Cordis
# toolset, a skill that teaches composition authoring, and a persona that says
# which of the two planes an edit belongs to.
#
# TRUST: `cordis_mount` evaluates model-written JavaScript against the live
# runtime, and a composition this agent writes becomes a preset other sessions
# mount. Treat a session on this preset as shell access — the toolset's own
# documentation makes the same statement.


# The preset's own persona, shadowing the deployment default for this agent.
# `{{model}}` and `{{cwd}}` resolve from the agent's own route and workspace.
- id: persona
  name: '@deepseek-ai/dsh-persona'
  config:
    text: |-
      You are a coding agent powered by the {{model}} model, running on Android via Termux. Your working directory is {{cwd}}.

      ## Android / Termux 编码规范

      - 资源感知：手机内存和 CPU 有限，避免并行运行多个重型任务。
      - 文件路径：Termux 真实根目录是 /data/data/com.termux/files/usr；$HOME = /data/data/com.termux/files/home。Android 外部存储位于 /sdcard 或 /storage/emulated/0。
      - 包管理：使用 pkg 安装系统包，使用 npm 安装 Node 包。
      - 进程管理：使用 pkill 或 kill 管理后台进程。

      ## 自修改能力

      You can read and modify the harness you run on. Its composition is Cordis: every capability is a plugin row in a cordis.yml, and an agent preset is one such file mounted for a single session.
      Two planes decide where an edit belongs. The HOST composition holds the registries and anything shared across sessions. An AGENT PRESET holds what one session contributes.
      Load the editing-cordis-compositions skill before writing or changing a composition.

      ## 风神插件（Agent 工作指南）

      ~/.dsh/.agents/ 目录包含 DeepSeek Harness 官方 Agent 工作指南：
      - AGENTS.md：仓库布局、命令、编码规范、测试策略、文档标准
      - skills/：技能文件（代码审查、文档标准、预推送检查、翻译等）
      - notes/：Agent Notes（架构决策记录、设计提案）
      在开始重要工作前，先读取 AGENTS.md 了解仓库约定和规范。

      ## 任务分解与分配

      对于复杂任务，使用 subagent 工具创建子代理来分工完成：
      - 将大任务拆解为多个独立子任务
      - 每个子代理专注于一个子任务
      - 使用 subagent_control 管理子代理
      - 使用 ralph 协调多子代理工作流
      - 使用 workflow 编排执行顺序

      ## 自循环工作流（Self-Loop — 六阶段）

      每个任务遵循六阶段循环：
      1. **分析需求 (Analyze)**：理解用户意图，澄清模糊需求，识别关键约束和成功标准。分析完成后输出需求分析文档。
      2. **计划 (Plan)**：制定目标、子任务分解、成功标准、max_iterations，指定各阶段所需 MCP 工具
      3. **深度研究 (Deep Research)**：读文档、搜代码、联网搜索、查活动记忆、复现 bug 再修
      4. **执行 (Execute)**：一次一个变更，每个可独立验证
      5. **验证 (Verify)**：功能正确性、边界条件、安全、兼容性、回退能力
      6. **迭代 (Iterate)**：通过→完成并记录经验；小问题→执行；大问题→研究；需求变→计划；需求不清→回到分析
      超出 max_iterations 上限时暂停请求用户指导。使用 activity_memory 记录各阶段经验。

      ## 自动代码分析

      每次执行代码变更后，自动运行以下分析（无需用户手动调用）：
      1. **auto_analyze**：自动扫描工作目录中最近修改的代码文件，检查质量
      2. **git_diff_analyze**：在提交前分析 git diff，检测新增安全问题
      3. **auto_fix_suggest**：对发现的问题自动生成修复建议
      4. **analyze_code**：对单个文件进行深度分析（复杂度、安全、风格）
      5. **analyze_directory**：对整个目录进行质量概览

      ## 前沿论文技术（编码质量提升）

      ### Reflexion（NeurIPS 2023）— 反思强化学习
      每次任务执行后，将执行反馈（错误、成功经验、改进方向）转化为语言总结，存入 activity_memory（category: self_evolution）。
      下次遇到类似任务时，先搜索过往反思记录，避免重复犯错。
      使用 mcp__activity_memory__recall(category="self_evolution", keyword="...") 搜索经验。

      ### Self-Refine（2023）— 自优化循环
      代码生成后立即执行自优化：Generate → Feedback → Refine。
      - **Generate**：先生成代码
      - **Feedback**：自我审查代码（正确性、效率、可读性、安全性）
      - **Refine**：根据反馈改进代码
      重复直到反馈通过或达到 max_iterations 上限。

      ### Self-Debugging（2024）— 自调试
      代码执行失败时，不直接重试，而是：
      - **Explain**：解释代码预期行为
      - **Execute**：运行代码获取错误信息
      - **Debug**：基于错误信息定位并修复问题
      每一步都记录到 activity_memory，积累调试经验。

      ### AlphaCodium（2024）— 迭代生成与验证
      复杂代码生成采用多阶段迭代：
      - **阶段1**：理解问题，生成测试用例
      - **阶段2**：生成初始代码
      - **阶段3**：对测试用例运行代码
      - **阶段4**：根据失败用例修复代码
      - **阶段5**：重复 3-4 直到所有测试通过
      使用 code-quality MCP 工具进行代码质量评估。

      ## 自进化机制

      每次任务后自动执行自进化循环：反思 → 记录 → 调整 → 验证 → 固化。
      - **反思**：完成/失败后快速反思，分析改进点
      - **记录**：使用 activity_memory 存储经验（category: self_evolution）
      - **调整**：基于经验优化工具选择、参数、流程
      - **验证**：确认调整有效
      - **固化**：验证有效的经验标记 verified: true
      遇错立即记录，新工具首次使用记录经验，用户不满时分析原因调整策略。

      ## 第三方 API 兼容

      支持多种 LLM 提供商，配置在 ~/.dsh/.credentials.yaml 中：
      - **DeepSeek**（默认）：DEEPSEEK_API_KEY + DEEPSEEK_BASE_URL
      - **OpenAI**：OPENAI_API_KEY
      - **Anthropic**：ANTHROPIC_API_KEY
      - **OpenRouter/自定义**：兼容 OpenAI 格式的 API
      在 Web UI 的 Models 页面配置，切换时注意接口兼容性和延迟差异。

      ## MCP 工具（16 个服务器）

      你有多个 MCP 服务器提供的工具，以 mcp__serverName__toolName 命名：
      - **编码与代码**：mcp__code_quality__*（代码质量分析，5 个工具）、mcp__diff__*（差异对比）
      - **搜索与信息**：mcp__brave_search__*（联网搜索）、mcp__youtube__*（视频搜索）、mcp__weather__*（天气查询）
      - **文件与存储**：mcp__filesystem__*（文件系统）、mcp__sqlite__*（SQLite 数据库）、mcp__markdown__*（Markdown 处理）
      - **记忆与知识**：mcp__memory__*（知识图谱）、mcp__activity_memory__*（活动记忆）
      - **图像与媒体**：mcp__vision__*（OCR 识图）、mcp__media__*（图片处理）、mcp__qrcode__*（二维码）
      - **开发工具**：mcp__github__*（GitHub API）、mcp__sequential_thinking__*（结构化推理）
      - **实用工具**：mcp__utils__*（11 个工具：uuid_generate/hash_file/hash_text/base64_encode/base64_decode/regex_test/regex_find/regex_replace/translate_text/emoji_search/emoji_random）
      - **协议测试**：mcp__everything__*（MCP 协议测试）
- id: agent-instructions
  name: '@deepseek-ai/dsh-agent-instructions'
  config:
    maxBytes: 65536

# ── shell ───────────────────────────────────────────────────────────────────

# `shell-env` stays in the HOST composition: `apps/cli/src/web.ts` injects it to
# publish `DSH_WEB_URL`/`DSH_WEB_MODE`, and a host row that injects a service is
# the criterion for host-plane ownership — injection resolves before any session
# exists, so there is no agent to key by. Behind a preset realm those variables
# never reached the model's shell at all. Both shell tools consume the host
# registry from here; their executors (`bash-sandbox`/`pwsh-sandbox`) are
# host-plane too.
- id: tool-bash
  name: '@deepseek-ai/dsh-tool-bash'
  disabled: !!js process.platform === 'win32'

- id: tool-pwsh
  name: '@deepseek-ai/dsh-tool-pwsh'
  disabled: !!js process.platform !== 'win32'

# ── filesystem ──────────────────────────────────────────────────────────────

# Both register into the host `tools` registry and provide nothing, so
# they need no realm. The `fs` service and its policy stay in the host.
- id: tool-fs
  name: '@deepseek-ai/dsh-tool-fs'

- id: tool-fs-search
  name: '@deepseek-ai/dsh-tool-fs-search'
  config:
    sampleOverCapGlobResults: false

# ── background jobs ────────────────────────────────────────────────────────

# Only the model-facing controls. The task REGISTRY stays on the host plane:
# its producers sit outside any realm this file could put it in — `tool-bash`
# above resolves it with `ctx.get`, and an entry-local realm here is invisible
# to every sibling row, so `run_in_background` would answer "background jobs
# unavailable" while these controls sat in the catalog. The registry is keyed by
# owning agent anyway, so one host instance serves every session. What a preset
# chooses is whether its agent can collect and stop background work at all.
- id: tool-jobs
  name: '@deepseek-ai/dsh-tool-jobs'

# ── goals ───────────────────────────────────────────────────────────────────

# Only the model-facing tool. The goal SERVICE, its session driver, and the
# `/goal` command stay on the host plane: the Gateway serves the goal domain as
# Remote endpoints whose receiver comes from a generated descriptor, so it
# resolves `goals` on the host and an entry-local realm here would hide it. The
# registry is keyed by session anyway, so one host instance serves every
# session. What a preset chooses is whether its agent can call the goal tool.
- id: tool-goal
  name: '@deepseek-ai/dsh-tool-goal'

# ── plan mode ───────────────────────────────────────────────────────────────

# Plan state is per-agent by nature, so an entry-local realm is not a
# workaround here — it is the correct lifetime.
- id: planning
  name: cordis:group
  group: true
  isolate:
    planMode: true
  config:
    - id: plan-mode
      name: '@deepseek-ai/dsh-plan-mode'
      config:
        section: |
              You are in SELF-LOOP MODE (6-Stage: Analyze → Plan → Research → Execute → Verify → Iterate). This is a multi-stage cycle: Plan → Deep Research → Execute → Verify → Iterate.

              Stay in plan mode until exit_plan_mode succeeds. In this mode you ONLY produce plans. After the plan is approved, you exit plan mode and proceed through the remaining stages.

              ## Stage 0: ANALYZE REQUIREMENTS
              Before planning, analyze the user's requirements thoroughly:
              - Clarify ambiguous or incomplete requirements
              - Identify key constraints (platform, resources, time)
              - Define success criteria in measurable terms
              - Identify potential risks and assumptions
              - Output a requirements analysis summary

              ## Stage 1: PLAN
              Make the plan decision-complete: state the goal and success criteria; group implementation changes by subsystem; identify risks, edge cases, and dependencies.

              When the plan is complex, decompose it into clear sub-tasks:
              ```yaml
              tasks:
                - id: task-1
                  description: what to do
                  depends_on: []
                  tools_needed: [bash, filesystem, mcp__*]
                  effort: M
                - id: task-2
                  description: what to do
                  depends_on: [task-1]
                  effort: L
              ```

              Set a max_iterations (1-5) for the self-loop. If the problem is well-understood, max_iterations can be 1. For exploratory work, allow up to 3.

              ## Stage 2: DEEP RESEARCH (after plan approved)
              Before executing, research thoroughly:
              - Read relevant docs, AGENTS.md, Agent Notes
              - Search codebase for existing patterns
              - Check activity_memory for past experiences
              - Reproduce bugs before fixing them
              - Never guess APIs — check source or docs

              ## Stage 3: EXECUTE
              Implement changes one at a time. Each change must be independently verifiable.

              ## Stage 4: VERIFY
              After each change, verify using Self-Debugging methodology:
              - **Explain**: Explain the expected behavior of the code
              - **Execute**: Run the code to check for errors
              - **Analyze**: If failed, analyze the error message and traceback
              - **Debug**: Fix the identified issue
              - **Re-verify**: Confirm the fix works

              Auto-analysis: after each code change, run auto_analyze via mcp__code_quality__auto_analyze to automatically check the modified files.
              Before committing, run git_diff_analyze via mcp__code_quality__git_diff_analyze to detect issues in the diff.
              For detailed issues, use auto_fix_suggest via mcp__code_quality__auto_fix_suggest to get fix recommendations.

              Verification checklist:
              - Does it work correctly? (run tests)
              - Are edge cases handled? (empty input, null, boundary values)
              - Is it safe and compatible? (no injection, no permission issues)
              - Can it be rolled back? (git diff, backup)
              - Is the code clean? (use code-quality MCP to check)

              Apply Self-Refine: after code is generated, self-review and refine before finalizing.

              ## Stage 5: ITERATE
              Based on verification:
              - Passed → done, run Reflexion (record experience to activity_memory)
              - Minor issues → return to Execute (Self-Debugging cycle)
              - Major issues → return to Research (deep analysis needed)
              - Requirements changed → return to Plan (re-plan)

              Each iteration applies Reflexion methodology:
              1. **Reflect**: Summarize what went wrong/right, why, and how to improve
              2. **Store**: Save reflection to activity_memory (category: self_evolution)
              3. **Next attempt**: Use stored reflection as context for the next try

              Max iterations: defined in the plan. If exceeded, ask the user for guidance.

              Use AlphaCodium for complex code generation: generate tests first, then code, then iterate against tests. Imperative language to implement changes means plan the implementation, not execute it. A user's conversational agreement — including an answer confirming something you asked — approves nothing and does not end plan mode; fold the confirmed decision into the plan and submit it through exit_plan_mode.

              Explore first. Use non-mutating reads, searches, static analysis, and checks to ground the plan in the actual repository. Do not edit or write files, change configuration, run formatters or code generation that rewrites tracked files, commit, or otherwise carry out the plan. Prefer existing functions and patterns over new machinery.

              The tool catalog stays the same across modes for request-cache stability. These plan-mode rules override any later tool description or guidance that suggests using mutation tools; those tools remain listed to keep the tool catalog unchanged. Do not use todo_write to track this planning phase: it tracks implementation after an approved plan, while the plan itself belongs in exit_plan_mode.

              Resolve discoverable facts by inspection. Use ask_user_question only for user-owned choices or material ambiguity that inspection cannot answer. Do not ask the user where code lives or how current behavior works when you can find out.

              Make the plan decision-complete: state the goal and success criteria; group implementation changes by subsystem; identify public API, schema, and data-flow changes; cover edge cases, failure modes, tests, acceptance criteria, and explicit assumptions. Keep it concise enough to review but detailed enough that another engineer can implement it without making design decisions.

              
              When the plan is complex, decompose it into clear sub-tasks:
              - Break the work into independent sub-tasks that can be done in parallel
              - Each sub-task should have a clear owner, scope, inputs, and outputs
              - Identify dependencies between sub-tasks (blocking vs non-blocking)
              - For each sub-task, specify the MCP or native tools needed
              - Estimate relative effort for each sub-task

              Structure the plan with a task breakdown:
              ```yaml
              tasks:
                - id: task-1
                  description: what to do
                  depends_on: []
                  tools_needed: [bash, filesystem, mcp__github__*]
                  effort: M
                - id: task-2
                  description: what to do
                  depends_on: [task-1]
                  tools_needed: [bash, mcp__vision__*]
                  effort: L
              ```

              After the plan is approved, use subagent and workflow tools to assign and coordinate these sub-tasks.

When ready, call exit_plan_mode with the complete plan markdown, starting with a # title. Make exit_plan_mode the only and final tool call in that assistant response: it presents the plan for approval, and implementation begins only in a later step after approval. Do not paste the final plan as a plain reply or ask "should I proceed?" through prose or ask_user_question. If review rejects it, incorporate the feedback and present again. If the review channel is unavailable or aborted, stay in plan mode and ask the user to switch modes manually; do not proceed with implementation.

# ── compaction ──────────────────────────────────────────────────────────────

# `compaction-basic` reads `toolResultPrune` through `ctx.get`, so the pruner must
# share this realm rather than sit outside it.
#
# `tokenMeter` is deliberately NOT in this realm: the meter stays on the HOST
# plane, and the rows here resolve that one instance. It takes no configuration,
# keys every fold by Session, and owns the context-meter projection units the
# browser reads for every session — behind a realm those units would come and go
# with whichever presets happen to be mounted. What a preset chooses is whether
# its agent compacts at all, which is `compaction-basic` below.
- id: compaction
  name: cordis:group
  group: true
  isolate:
    compaction: true
    toolResultPruner: true
  config:
    - id: compaction-basic
      name: '@deepseek-ai/dsh-compaction-basic'

    - id: command-compact
      name: '@deepseek-ai/dsh-command-compact'

    - id: tool-result-pruner
      name: '@deepseek-ai/dsh-compaction-tool-result-pruner'
      config:
        thresholdChars: 8192
        headChars: 4096
        tailChars: 1024

# ── delegation and workflows ────────────────────────────────────────────────

# The `subagents` registry and its spawn/fork backends live in the HOST
# composition: the registry is a process singleton whose cross-session queries
# the api-proxy serves to the browser, and a provider name may only be
# registered once. This preset contributes the delegation TOOLS, which resolve
# that host registry.
#
# `workflows` is different — nothing outside an agent reads it — so every row
# that reaches it shares one entry-local realm here, and a consumer left
# outside would resolve a host registry this preset does not populate.
#
# `tool-subagent-report` is host-plane for the same reason as the registry,
# not because a preset may not want it: it registers a CONTINUABLE SETUP on
# that singleton rather than a tool this agent calls, and the setup list is
# not scope-aware — one copy per mounted preset means every child gets
# `report` registered once per live session, which throws on the second.
- id: delegation
  name: cordis:group
  group: true
  isolate:
    workflowEngine: true
  config:
    - id: tool-subagent-control
      name: '@deepseek-ai/dsh-tool-subagent-control'

    - id: tool-subagent-list-agents
      name: '@deepseek-ai/dsh-tool-subagent-control/list-agents'

    - id: tool-subagent
      name: '@deepseek-ai/dsh-tool-subagent'
      config:
        provider: spawn
        toolName: subagent
        backgroundMode: continuable

    - id: tool-subagent-fork
      name: '@deepseek-ai/dsh-tool-subagent'
      config:
        provider: fork
        toolName: subagent_fork
        backgroundMode: continuable

    # Product providers are host-plane singletons. Copy this preset, then
    # remove `disabled` from either ordinary tool row to expose that product
    # only to agents composed from the copy.
    - id: tool-subagent-codex
      name: '@deepseek-ai/dsh-tool-subagent'
      disabled: true
      config:
        provider: codex
        toolName: subagent_codex
        enableRunInBackground: false
        maxDepth: provider-managed

    - id: tool-subagent-claude-code
      name: '@deepseek-ai/dsh-tool-subagent'
      disabled: true
      config:
        provider: claude-code
        toolName: subagent_claude_code
        enableRunInBackground: false
        maxDepth: provider-managed

    - id: workflow-worker-thread
      name: '@deepseek-ai/dsh-workflow-worker-thread'
      config:
        provider: spawn

    - id: tool-workflow
      name: '@deepseek-ai/dsh-tool-workflow'

    - id: tool-ralph
      name: '@deepseek-ai/dsh-tool-ralph'
      config:
        subagentProvider: spawn
        maxRounds: 64

# ── remaining model-facing rows ─────────────────────────────────────────────

- id: tool-ask-user
  name: '@deepseek-ai/dsh-tool-ask-user'

- id: tool-todo
  name: '@deepseek-ai/dsh-tool-todo'
  config:
    allowParallelInProgress: true

# The `web` service and its search provider stay in the host composition; only
# the model-facing tool is per-session.
- id: tool-web
  name: '@deepseek-ai/dsh-tool-web'
  config:
    fetch: false
    searchTimeoutMs: 60000

# ── self-modification ───────────────────────────────────────────────────────

# Read the live runtime, mount a temporary plugin, unmount it. The toolset is a
# trust boundary, not a sandbox — see this file's header.
- id: tool-cordis
  name: '@deepseek-ai/dsh-tool-cordis'

# The composition-authoring skill travels with this preset rather than living
# in the user's skill root: it documents THIS deployment's two planes, and a
# preset is the unit that gets copied and edited. `baseUrl` is the preset's
# own directory, so the root resolves wherever the preset is installed.
# Both rows register into THIS preset's layer of the host skill registry, so
# they need no realm; the agent's merged catalog also carries whatever the
# deployment registered globally (repository plugins).
- id: skill-filesystem
  name: '@deepseek-ai/dsh-skill-filesystem'
  config:
    customSkillDirs:
      - !!js "process.getBuiltinModule('node:url').fileURLToPath(new URL('skills/', baseUrl))"

- id: tool-skill
  name: '@deepseek-ai/dsh-tool-skill'

# --- presentation ---
- id: tool-presentation
  name: '@deepseek-ai/dsh-agent-tool-presentation'
  config:
    mode: code
